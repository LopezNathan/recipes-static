# Cloudflare Pages configuration.
# The site is fully static; `dist/` (including the generated /pagefind index)
# is the build output Pages serves. Deploy with:
#   npm run build && npx wrangler pages deploy dist
name = "recipes-static"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
